Semantic View is not officially Snowflake supported.
Agent assumes that Documentation CKE has already been installed in the account (per standard quickstart)

Note that the role and warehouse in the code may need updated to match your use case.
